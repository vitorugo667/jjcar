{
  "framework": "nextjs",
  "installCommand": "echo skip",
  "buildCommand": "npx pnpm@8 install --no-frozen-lockfile && npx pnpm@8 --filter @jjcar/db exec prisma generate && cd apps/web && npx next build",
  "outputDirectory": "apps/web/.next"
}
